# ARCHIVE INSTRUCTIONS

**The `docs/archive/` folder is for reference only, not authoritative, superseded by current Design, Specs, and Results.**

Artifacts move here once their work is complete and they are no longer a live reference.
Retirement while work is still live is a `Status:` edit, not a move — see `CLAUDE.md`
§ Documentation Standards.

This folder is git-tracked, so citations into it stay resolvable. Never cite anything here
as the basis for a current decision; if it still governs something, it belongs back in
`docs/dev/`.
