# Fixture Standards File

### 1.3 Issue discipline

- Labels carry area, and `alpha`/`beta` is the label pair that says what an unscheduled issue is. An issue with no milestone carries exactly one of them.
- A milestone carries the exit condition that closes it.

---

## 2. Git Workflow
